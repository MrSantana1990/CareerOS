# Packages

Fronteiras planejadas: `domain`, `shared`, `ai`, `browser-automation`, `integrations`, `observability` e `config`. Elas serão criadas quando houver comportamento real para evitar abstrações vazias na fundação.

